using System;
using FMODUnity;
using Game.Code.Infrastructure.GameMachine;
using UnityEngine;
using Zenject;

namespace Game.Code.GameLogick
{
    public class EnemyGreen : Enemy
    {
        /*private StudioEventEmitter emitter;

        [Inject] SoundManager soundManager;
        [Inject] private FMODEvents _fmodeEvents;

        private void Start()
        {
            emitter = soundManager.InitializeEventEmitter(_fmodeEvents.EnemyRoar, this.gameObject);

            emitter.Play();
        }*/
    }
}